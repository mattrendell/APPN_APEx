# File Structure

# Repository root
NodeSummary.yaml			# user-maintained: defines nodes and SensorPlatforms
FolderStructureInfo.txt		# this file (reference for naming convention)

node

	{NodeName}_ProjectsSummary.csv	# auto: boolean matrix of projects vs sensors

	YYYY_ProjectDesc{_internal/external}{_Researcher}{_org} 

		ProjectSummary.yaml	# auto: project metadata, researcher, sites
		FieldLog.csv		# auto: log of every field collection event

		Site{_source}
			
			Documentation
				Plot_Layout
					{YYYYSiteName}_plots.geojson						# mandatory main plot file (not auto-created)
					{YYYYSiteName}_plots_{descriptor|sensor}[_v{NN}].geojson	# optional variants (e.g. _unbuffered, _HIRES)
					{YYYYSiteName}_sampling_{measurement}_{YYYYMMDD}[_v{NN}].geojson	# optional, one per measurement type
					{YYYYSiteName}_gcp[_v{NN}].geojson					# optional, permanent surveyed GCPs only
					{YYYYSiteName}_plots_v{NN}.json						# optional tool config sidecar (e.g. FIELDimageR)
					*_deprecated.geojson							# superseded copies retained for provenance
					README.md								# auto: seeded template, strongly recommended to maintain
				Trial_Info
					{YYYYSiteName}_trial_info.csv						# trial-info spreadsheet; must contain plot_id column
					{YYYYSiteName}_trial_info_{YYYYMMDD}_deprecated.csv			# superseded versions
					README.md								# auto: seeded template
			Code

			SensorPlatform

				YYYYMMDD 

					FieldNotes.txt		# auto (optional): free-form day notes
					RunOverview.csv		# auto (optional): per-run status table

					runXX

						T0_raw
							*Vault		# GOBI / CALVIS only — excluded from programmatic cleanup
						T1_proc
							*QC_data	# GOBI / CALVIS only — quality-control inputs/outputs
						T2_traits

		*Documents
		*code
	Documents
	code

# Notes on metadata files
# - All auto files are created and maintained by ProjectBuilder.py.
# - FieldNotes.txt and RunOverview.csv are skipped when the corresponding
#   FieldLog.csv row sets MakeNotesFile / MakeTableFile to False.
# - RunOverview.csv has one row per run_XX and three status trigger bools
#   (all default False; ProjectBuilder appends missing columns to existing
#   tables without touching values or user-added columns):
#     RunFailed  - no recoverable data for the entire run (may be set late,
#                  e.g. a GNSS failure only found during processing)
#     Issues     - something went wrong but recoverable data exists
#                  (partial sub-sensor loss, QC failures, degraded quality)
#     Deviations - no fault, but the capture plan deviated from the standard
#                  full capture (e.g. an intended RGB-only flight)
#   Decision rule: planned it that way -> Deviations; went wrong but kept
#   data -> Issues; run is a write-off -> RunFailed. Bools may combine.
# - Site-level Documentation/Plot_Layout/ and Documentation/Trial_Info/
#   are auto-created at each site; their README.md stubs are seeded once
#   and never overwritten. See the Plot Delineation protocol for the
#   full file-naming spec and the _deprecated / _v{NN} conventions.
# - See the wiki Key-Files page for full schema details.

# Summary 
# Separator
use underscore to separate blocks of information 


# Optional Arguments
{_*} a parameter that can be included but isn't required. It must be possible to programmatically identify these arguments.


*Documents & *code
Optional folders used to store relevant information for that level of the folder structure


# Overview of Arguments
#+++++

node
	name of node with the UNI abbr first
	e.g. USYD_Narrabri

#+++++

YYYY
	year a project started
	e.g. 2025

ProjectDesc
	   a short description of the project with no spaces
	   e.g. SIFcalibration

internal/external
	Optional Argument
	Describe if a project is internal or external Using IorE
	e.g. E

Researcher
	Optional Argument
	Last Name of lead researcher, using Title Case
	e.g. Salter

org
	Optional Argument
	internal code for an organisation (important for UQ). Should all be caps and a fixed length
	e.g. ABC

#+++++

Site	
	Name or code of a site

Source
	   Optional Argument
	   F for field, C for controlled environment
	   e.g. F

#+++++
SensorPlatform
	Short Name of sensor platform. For field data (e.g. Biomass cuts) the SensorPlatform is called GroundTruth
	e.g. GOBI

#+++++
YYYYMMDD
	   date of the field work or data collection. Leading zeros included for months and days
	   Note. For things like a weather station or soil moisture probes it can be the first/last day of the month for each folder containing 1 month's data, or the date the logging started for all data in 1 folder, or the date of data retrieval for inconsistent collection.  
	   e.g. 20250101


#+++++
run 
	   the number of blocks of data to be processed together. runXX where X is a number. Includes leading zeros and is zero-indexed (00, 01, 02)
	   Note. For drones, this is individual flights; for soil radar, it would be one pass with the tractor, etc.
	   e.g. run00


